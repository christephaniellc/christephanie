// jest.setup.ts
import '@testing-library/jest-dom/extend-expect'; // Extends Jest with additional matchers

// If you decide to use MSW in the future, you can set it up here
// import { server } from './src/mocks/server';

// beforeAll(() => server.listen());
// afterEach(() => server.resetHandlers());
// afterAll(() => server.close());
