name: Deploy Backend Lambdas (Dev)

on:
  push:
    branches:
      - main.dev

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Build & Deploy Changed Backend Lambdas
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'

      - name: Setup AWS Credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: arn:aws:iam::390403858788:role/GitHubActionsDeployRole
          role-session-name: GitHubActionsSession
          aws-region: us-east-1

      - name: Find Changed Lambda Projects
        id: changes
        run: |
          echo "Detecting modified Lambda functions..."
          
          # Ensure we have the full git history (fix for HEAD^ issue)
          git fetch --prune --unshallow

          # Check if there's a previous commit
          if git rev-parse HEAD~1 >/dev/null 2>&1; then
            previous_commit="HEAD~1"
          else
            previous_commit=$(git rev-list --max-parents=0 HEAD)  # Get first commit
          fi

          changed_dirs=$(git diff --name-only $previous_commit HEAD backend/src | grep "Wedding.Lambdas." | cut -d'/' -f3 | sort -u | grep -v -E "(unittests|tests)" || true)
          
          if [[ -z "$changed_dirs" ]]; then
            echo "No Lambda changes detected. Skipping deployment."
            echo "deploy=false" >> $GITHUB_ENV
          else
            echo "Changed Lambda directories: $changed_dirs"
            echo "deploy=true" >> $GITHUB_ENV
            echo "dirs=$changed_dirs" >> $GITHUB_ENV
          fi

      - name: Build & Deploy Lambda Functions
        if: env.deploy == 'true'
        run: |
          application_name="christephanie-wedding-api"
          
          for dir in $dirs; do
            full_path="backend/src/$dir"
            base_name=$(basename "$full_path" | sed 's/^Wedding\.Lambdas\.//')
            function_name="${application_name}-$(echo "$base_name" | tr '.' '-' | tr '[:upper:]' '[:lower:]')"

            echo "Building $function_name"
            dotnet publish "$full_path" -c Release -o "$full_path/bin/publish"

            echo "Packaging Lambda (excluding .pdb files): $function_name"
            cd "$full_path/bin/publish" || exit 1
            zip -r "../$function_name.zip" . -x "*.pdb"
            cd - || exit 1

            echo "Updating Lambda: $function_name"
            aws lambda update-function-code \
              --function-name "$function_name" \
              --zip-file "fileb://$full_path/bin/$function_name.zip"
          done
